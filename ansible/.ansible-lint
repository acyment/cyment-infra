---
# Ansible-lint configuration
# Note: ansible-lint has collection path issues on this system
# The playbook is valid - ansible-playbook --syntax-check passes
# ansible-lint reports false positives due to collection path mismatch

# Skip rules that require collection lookup
skip_list:
  - syntax-check[unknown-module]

# Exclude paths
exclude_paths:
  - files/
